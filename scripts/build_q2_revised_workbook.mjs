import fs from 'node:fs/promises';
import {Workbook,SpreadsheetFile} from '@oai/artifact-tool';
const out='D:/2026MMC/outputs/q2_revised';
const parse=async n=>{const s=(await fs.readFile(out+'/'+n,'utf8')).replace(/^\uFEFF/,'').trim();return s.split(/\r?\n/).map(line=>{let a=[],v='',q=false;for(let i=0;i<line.length;i++){const c=line[i];if(c==='"')q=!q;else if(c===','&&!q){a.push(v);v=''}else v+=c}a.push(v);return a.map((x,j)=>x===''||x.toLowerCase()==='nan'?'':(j===0||!Number.isFinite(Number(x))?x:Number(x)))})};
const input=await parse('../q1_final_rebuilt/q1_model_input.csv'),validation=await parse('q2_model_validation.csv'),forecast=await parse('q2_forecast_2026_2030.csv'),sens=await parse('q2_arrivals_imputation_sensitivity.csv'),covid=await parse('q2_covid_counterfactual.csv'),audit=await parse('q2_bootstrap_residual_audit.csv'),stationarity=await parse('q2_stationarity_tests.csv'),orders=await parse('q2_arima_order_comparison.csv');
for(const r of input.slice(1)){if(Number(r[0])===2010)r[11]='统计公报转载值_政府报告交叉验证';if(Number(r[0])===2025){r[6]=2691;r[7]=2691;r[9]=200;r[10]='官方媒体补充_准留出';r[11]='官方媒体补充_准留出'}}
const wb=Workbook.create();wb.comments.setSelf({displayName:'User'});const head='#17365D',sub='#D9EAF7';
function sheet(name,data,widths=[]){const s=wb.worksheets.add(name);s.showGridLines=false;s.getRangeByIndexes(0,0,data.length,data[0].length).values=data;s.getRangeByIndexes(0,0,1,data[0].length).format={fill:head,font:{bold:true,color:'#fff'},wrapText:true};s.freezePanes.freezeRows(1);for(let i=0;i<data[0].length;i++)s.getRangeByIndexes(0,i,data.length,1).format.columnWidth=widths[i]||17;s.getUsedRange().format.rowHeight=21;return s}
const intro=[['项目','修订后结论'],['数据口径','2010收入=35.5亿元；2021/22游客量为主插补；2025旅游数据为官方媒体补充'],['基准模型','带漂移ARIMA(0,1,0)'],['平稳性检验','已补充ADF与KPSS；原对数和一阶差分结果见“平稳性检验”'],['阶数比较','已比较p,q=0—2的9种ARIMA(p,1,q)，见“ARIMA阶数比较”'],['2025游客量准留出','预测2866.56，补充值2691，APE=6.52%'],['2025旅游收入准留出','预测251.84，补充值200，APE=25.92%'],['2030基准预测','游客量3954.45万人次；旅游收入355.87亿元'],['区间设计','基准区间排除2020极端残差后重新中心化；压力区间保留全部残差并中心化'],['敏感性','插补不改变点预测，但会改变Bootstrap区间'],['COVID解释','系数表示年度增长率影响；水平损失使用反事实路径另算'],['使用限制','年度小样本；2025旅游值不是严格政府统计实测；远期区间较宽']];
const s0=sheet('结论说明',intro,[24,95]);s0.getRange('A2:A12').format={fill:sub,font:{bold:true}};s0.getRange('B2:B12').format.wrapText=true;
const s1=sheet('建模数据',input,[10,15,15,15,15,18,18,20,18,18,25,22]);s1.getRange('B2:J17').format.numberFormat='0.00';
const s2=sheet('模型验证',validation,[23,24,18,18,18,18,18,18,16]);s2.getRange('C2:H5').format.numberFormat='0.00';s2.getRange('I2:I5').format.numberFormat='0.00%';
const s3=sheet('预测结果',forecast,[23,14,10,18,18,18,16,16]);s3.getRange('D2:F21').format.numberFormat='0.00';s3.getRange('G2:H21').format.numberFormat='0.00%';
const s4=sheet('插补敏感性',sens,[18,10,18,18,18,16,16]);s4.getRange('C2:E11').format.numberFormat='0.00';s4.getRange('F2:G11').format.numberFormat='0.00%';
const s5=sheet('COVID反事实',covid,[23,19,16,10,22,22,18]);s5.getRange('B2:C7').format.numberFormat='0.00%';s5.getRange('E2:F7').format.numberFormat='0.00';s5.getRange('G2:G7').format.numberFormat='0.00%';
const s6=sheet('Bootstrap审计',audit,[23,14,16,14,26,26]);s6.getRange('E2:F5').format.numberFormat='0.000000';
const s7=sheet('平稳性检验',stationarity,[23,18,12,14,14,12,15,15,15,31,14,12,12,16,16,16,31,34]);s7.getRange('D2:D5').format.numberFormat='0.0000';s7.getRange('G2:I5').format.numberFormat='0.0000';s7.getRange('K2:K5').format.numberFormat='0.0000';s7.getRange('N2:P5').format.numberFormat='0.0000';
const s8=sheet('ARIMA阶数比较',orders,[23,8,8,8,18,17,12,12,12,20,24,18,22,22,20,19,38]);s8.getRange('F2:L19').format.numberFormat='0.0000';
for(const [name,key,title,unit] of [['图_游客量','游客接待量_主插补','游客接待量：2026—2030基准与压力区间','万人次'],['图_旅游收入','旅游综合收入','旅游综合收入：2026—2030基准与压力区间','亿元']]){
 const s=wb.worksheets.add(name);s.showGridLines=false;const rows=forecast.slice(1).filter(r=>r[0]===key);const yrs=[2026,2027,2028,2029,2030];const data=[['年份','点预测','基准下界','基准上界','压力下界','压力上界'],...yrs.map(y=>{const b=rows.find(r=>r[1]==='baseline'&&r[2]===y),t=rows.find(r=>r[1]==='stress'&&r[2]===y);return[y,b[3],b[4],b[5],t[4],t[5]]})];s.getRange('A1:F6').values=data;s.getRange('A1:F1').format={fill:head,font:{bold:true,color:'#fff'}};s.getRange('A:F').format.columnWidth=16;const c=s.charts.add('line',s.getRange('A1:F6'));c.title=title;c.hasLegend=true;c.xAxis={axisType:'textAxis'};c.yAxis={numberFormatCode:'#,##0'};c.yAxis.title.text=unit;c.setPosition('H2','Q23');
}
const err=await wb.inspect({kind:'match',searchTerm:'#REF!|#DIV/0!|#VALUE!|#NAME\\?|#N/A',options:{useRegex:true,maxResults:100},summary:'errors'});console.log(err.ndjson);
for(const n of ['结论说明','建模数据','模型验证','预测结果','插补敏感性','COVID反事实','Bootstrap审计','平稳性检验','ARIMA阶数比较','图_游客量','图_旅游收入']){const b=await wb.render({sheetName:n,autoCrop:'all',scale:1,format:'png'});await fs.writeFile(out+'/preview_'+n+'.png',new Uint8Array(await b.arrayBuffer()))}
const x=await SpreadsheetFile.exportXlsx(wb);await x.save(out+'/第二问_ARIMA修订建模结果.xlsx');
console.log((await wb.inspect({kind:'table',range:'结论说明!A1:B12',include:'values,formulas',tableMaxRows:14,tableMaxCols:3})).ndjson);
